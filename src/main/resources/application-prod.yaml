# =====================================================================
# prod 프로필 — 운영 고유 오버라이드만.
# (공유 원격 설정은 application-cloud.yaml, group 으로 함께 활성화됨)
# =====================================================================

# 운영에서는 API 문서 노출 차단(정보 노출 방지). ALB 리스너 규칙 차단은 방어심화용 보조.
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false
