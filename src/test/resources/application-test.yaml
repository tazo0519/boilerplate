# =====================================================================
# test 프로필 — 운영 jar(src/main) 에 실리지 않도록 test 리소스에 위치.
# 데이터소스는 Testcontainers(@ServiceConnection)가 주입하므로 여기서 정의하지 않는다.
# 스키마는 운영과 동일하게 Flyway(db/migration)가 부팅 시 적용하며,
# ddl-auto/dialect 는 application.yaml 공통값(validate / PostgreSQL)을 상속한다.
# ※ 실행에 Docker 필요.
# =====================================================================

# TEST ONLY — 로컬과 동일한 더미 키(가짜). 운영 재사용 금지.
boilerplate:
  security:
    crypto:
      key-base64: "TG9jYWxPbmx5RG9Ob3RVc2VJblByb2QhMTIzNDU2Nzg="
